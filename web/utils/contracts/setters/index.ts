import {
  createCompany,
  updateEcoScore,
  updateEmissions,
} from "./company_registry";
import { registerDevice, deactivateDevice } from "./device_registry";
import { submitReport, updateFlightPath } from "./emissions_report";
import { transfer, transferFrom, approve, mint, burn } from "./token_contract";

export {
  createCompany,
  updateEcoScore,
  updateEmissions,
  registerDevice,
  deactivateDevice,
  submitReport,
  updateFlightPath,
  transfer,
  transferFrom,
  approve,
  mint,
  burn,
};
